<!DOCTYPE html>
<html>

<head>
  <title>deleteSessions</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <h4 id="user">Вход выполнен через учетную запись: <%=username %></h4>
  <% if (message) { %>
  <h4><%=message %></h4>
  <% } %>
  <form method='POST' action="api/sessions/delete">
    <h4>Удалить сессию</h4>
    <input type="number" name="deleteId" placeholder="Id сессии" required autocomplete="off" />
    <button type='submit'>Удалить сессию</button>
  </form>

  <table>
    <caption>Список сессий</caption>
    <tr>
      <th>Номер сесcии</th>
      <th>sessionId</th>
      <th>UserId</th>
    </tr>

    <% for (let session of sessions) { %>
    <tr>
      <td><%=session.id %></td>
      <td><%=session.sessionId %></td>
      <td><%=session.UserId %></td>
    </tr>
    <%} %>

  </table>

  <h1><a href="admin">Назад</a></h1>
  <h1><a href="/">На главное меню</a></h1>

</body>

</html>