<!DOCTYPE html>
<html>

<head>
  <title>deleteUser</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <h4 id="user">Вход выполнен через учетную запись: <%=username %></h4>
  <% if (message) { %>
  <h4><%=message %></h4>
  <% } %>
  <form method='POST' action="api/user/delete">
    <h4>Удалить пользователя</h4>
    <input type="number" name="deleteId" placeholder="Id пользователя" required autocomplete="off" />
    <button type='submit'>Удалить пользователя</button>
  </form>
  <table>
    <caption>Список пользователей</caption>
    <tr>
      <th>ID пользователя</th>
      <th>Username</th>
      <th>Password</th>
      <th>Id роли</th>
    </tr>

    <% for (let user of users) { %>
    <tr>
      <td><%=user.id %></td>
      <td><%=user.username %></td>
      <td><%=user.password %></td>
      <td><%=user.roleId %></td>
    </tr>
    <%} %>

  </table>

  <h1><a href="admin">Назад</a></h1>
  <h1><a href="/">На главное меню</a></h1>

</body>

</html>