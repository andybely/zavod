<!DOCTYPE html>
<html>

<head>
  <title>sessions</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <h4 id="user">Вход выполнен через учетную запись: <%=username %></h4>
  <form method="POST" action="">
    <input name="s" placeholder="Поиск по номеру сессии" type="search">
    <button type="submit">Поиск</button>
  </form>
  <table>
    <caption>Список сессий</caption>
    <tr>
      <th>Номер сесии</th>
      <th>sessionId</th>
      <th>UserId</th>
    </tr>

    <% for (let session of sessions) { %>
    <tr>
      <td><%=session.id %></td>
      <td><%=session.sessionId %></td>
      <td><%=session.UserId %></td>
    </tr>
    <%} %>

  </table>
  <h1><a href="/">На главное меню</a></h1>

</body>

</html>