<!DOCTYPE html>
<html>

<head>
  <title>products</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <h4 id="user">Вход выполнен через учетную запись: <%=username %></h4>
  <form method="GET" action="/products">
    <input name="productsId" placeholder="Поиск по ID изделия" type="search">
    <button type="submit">Поиск</button>
  </form>
  <form method="GET" action="/findByNominations">
    <input name="nominations" placeholder="Поиск по наименованию" type="search">
    <button type="submit">Поиск</button>
  </form>
  <form method="GET" action="/findBySectorId">
    <input name="SectorsId" placeholder="Поиск по ID сектора" type="search">
    <button type="submit">Поиск</button>
  </form>
  <table>
    <caption>Строительные изделия</caption>
    <tr>
      <th>ID строильного изделия</th>
      <th>Наименование</th>
      <th>Количество</th>
      <th>Брак</th>
      <th>Описание</th>
      <th>Id Сектора</th>
    </tr>

    <% for (let product of products) { %>
    <tr>
      <td><%=product.id %></td>
      <td><%=product.nomination %></td>
      <td><%=product.quantity %></td>
      <td><%=product.defect %></td>
      <td><%=product.description %></td>
      <td><%=product.SectorId %></td>
    </tr>
    <%} %>

  </table>
  <h1><a href="/">На главное меню</a></h1>

</body>

</html>