<!DOCTYPE html>
<html>

<head>
  <title>sessions</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <% if (!username) { %>
  <h3>Ты не залогинен чорт!</h3>
  <h1><a href="login">Вход</a></h1>
  <% } else{ %>
  <h4>Вход выполнен через учетную запись: <%=username %></h4>
  <table>
    <caption>Список сессий</caption>
    <tr>
      <th>Номер</th>
      <th>sessionId</th>
      <th>UserId</th>
    </tr>

    <% for (let session of sessions) { %>
    <tr>
      <td><%=session.id %></td>
      <td><%=session.sessionId %></td>
      <td><%=session.UserId %></td>
    </tr>
    <%} %>

  </table>
  <h1><a href="/">На главное меню</a></h1>

  <% } %>
</body>

</html>