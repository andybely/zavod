<!DOCTYPE html>
<html>

<head>
  <title>createProducts</title>
  <script src="/assets/jquery.min.js"></script>
  <link rel="stylesheet" href="/assets/css/main.css">
</head>

<body>

  <h4 id="user">Вход выполнен через учетную запись: <%=username %></h4>
  <% if (message) { %>
  <h4><%=message %></h4>
  <% } %>
  <form method='POST' action="api/products/create">
    <h4>Добавление нового строильного изделия</h4>
    <input type="text" name="nomination" placeholder="Наименование" required autofocus="" autocomplete="off" />
    <input type="number" name="quantity" placeholder="Количество" required autocomplete="off" />
    <input type="number" name="defect" placeholder="Брак" required autocomplete="off" />
    <input type="text" name="description" placeholder="Описание" required autofocus="" autocomplete="off" />
    <input type="number" name="sectorId" placeholder="Id Сектора" required autocomplete="off" />
    <button type='submit'>Добавить строительное изделие</button>
  </form>

  <table>
    <caption>Строительные изделия</caption>
    <tr>
      <th>ID строильного изделия</th>
      <th>Наименование</th>
      <th>Количество</th>
      <th>Брак</th>
      <th>Описание</th>
      <th>Id Сектора</th>
    </tr>

    <% for (let product of products) { %>
    <tr>
      <td><%=product.id %></td>
      <td><%=product.nomination %></td>
      <td><%=product.quantity %></td>
      <td><%=product.defect %></td>
      <td><%=product.description %></td>
      <td><%=product.SectorId %></td>
    </tr>
    <%} %>

  </table>
  
  <h1><a href="admin">Назад</a></h1>
  <h1><a href="/">На главное меню</a></h1>

</body>

</html>